name: konecte_apps
description: "A new Flutter project."

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ^3.5.4

dependencies:
  flutter:
    sdk: flutter

  # Dependencias para Firebase y Firestore
  firebase_core: ^3.8.0
  firebase_auth: ^5.3.3
  cloud_firestore: ^5.5.0

  # Iconos estilo iOS
  cupertino_icons: ^1.0.8

dev_dependencies:
  flutter_test:
    sdk: flutter

  # Buenas prácticas de lint
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true

  # Configuración de assets
  assets:
    - assets/images/  # Carpeta que contiene tus imágenes

